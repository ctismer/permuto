"""permuto - a modern (PySide6) reincarnation of the 1990-95 Modula-2
Polytop/Permutograph viewer.

Phase 1 is a faithful, verifiable port of the original: the generation
pipeline (``genperm`` -> ``operate`` -> ``num2``) is reproduced exactly and
checked against the original ``.pg``/``.nod`` files as golden tests. See
``docs/ARCHITECTURE.md``.
"""

__version__ = "0.0.1"
