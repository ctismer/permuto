"""Standalone experiments from the 1990s source, kept for their own sake.

These import nothing from the viewer and the viewer imports nothing from them.
They are here because they are part of the recovered work, not because the
permutograph needs them.
"""

from .kugel import PALETTE, render_sphere

__all__ = ["PALETTE", "render_sphere"]
